import 'package:flutter/material.dart';

abstract class AppColors {
  static const lblue = Colors.blue;
  static const textDark = Colors.black87;
  static const textLight = Colors.white;
  static const textFaded = Colors.grey;
  static const iconLight = Colors.white;
  static const iconDark = Colors.grey;
  static const cardLight = Color(0xFFF9FAFE);
  static const cardDark = Color.fromRGBO(70, 83, 158, 1);
}
